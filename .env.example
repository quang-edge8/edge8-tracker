# Copy to .env and fill in after M1 (registering the GitHub App).
# The real .env is gitignored — never commit it.

# GitHub App credentials
APP_ID=
WEBHOOK_SECRET=
PRIVATE_KEY_PATH=keys/app.private-key.pem

# Local server
PORT=3000
